{
  "name": "RNVideoConf",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "android": "react-native run-android",
    "ios": "react-native run-ios",
    "start": "react-native start",
    "test": "jest",
    "lint": "eslint .",
    "android:rerun": "npx jetify && yarn run android:clean && yarn run android",
    "android:clean": "cd android && ./gradlew clean && ./gradlew cleanBuild && ./gradlew cleanBuildCache"
  },
  "dependencies": {
    "react": "16.11.0",
    "react-native": "0.62.2",
    "react-native-connectycube": "3.0.6",
    "react-native-webrtc": "1.75.3"
  },
  "devDependencies": {
    "@babel/core": "^7.9.6",
    "@babel/runtime": "^7.9.6",
    "@react-native-community/eslint-config": "^1.1.0",
    "babel-jest": "^26.0.0",
    "eslint": "^6.8.0",
    "jest": "^26.0.0",
    "metro-react-native-babel-preset": "^0.59.0",
    "react-test-renderer": "16.11.0"
  },
  "jest": {
    "preset": "react-native"
  }
}
